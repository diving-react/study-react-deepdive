/**
 * @fileoverview 원시타입 undefined
 * @description 원시타입 중 하나인 undefined는 값이 없음을 나타낸다. 엄밀히 말하면 값이 없는 것이 아니라 값이 할당되지 않은 것이다.
 */
let foo;

type of foo === 'undefined'; // true

function bar() {
  return hello;
}

typeof bar() === 'undefined'; // true